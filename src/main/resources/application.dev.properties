#config db
spring.profiles.active=dev
spring.jpa.database=POSTGRESQL
spring.datasource.drive-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${PROD_DB_HOST}:${PROD_DB_PORT}/${PROD_DB_NAME}
spring.datasource.username=${PROD_DB_USERNAME}
spring.datasource.password=${PROD_DB_PASSWORD}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.hibernate.ddl-auto=update


allowed.origin=http://localhost:4200
# Ajustar la memoria de la JVM
# Configuración de memoria JVM
#spring-boot.run.jvmArguments=-Xms256m -Xmx512m -XX:MaxDirectMemorySize=10M -XX:MaxMetaspaceSize=256M -XX:ReservedCodeCacheSize=240M -Xss1M
